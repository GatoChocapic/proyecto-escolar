/**
 * Configuración del entorno de producción.
 * Cambiar apiUrl al dominio real de tu servidor.
 */
export const environment = {
  production: true,
  apiUrl: 'https://proyecto-escolar.onrender.com/api/',
};
